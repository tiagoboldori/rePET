# /etc/replay-system/cameras/loc1-quadra3.env
# Um arquivo destes por câmera. O nome do arquivo (sem .env) = quadra_id,
# usado como instância do unit: systemctl enable --now replay-capture@loc1-quadra3
INPUT_URL=rtsp://user:senha@10.0.1.13:554/stream1
BUFFER_ROOT=/var/replay
SEGMENT_TIME=2
